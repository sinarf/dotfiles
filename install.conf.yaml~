- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf

- create:
    - ~/dev
    - ~/.local/share/vim/{swap,backup,undo}

- shell:
  - [git submodule update --init --recursive, Installing submodules]
